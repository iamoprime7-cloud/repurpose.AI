# repurpose.AI
converts YouTube videos into ready to publish content. It extracts audio, processes it with Google Gemini, and automatically generates LinkedIn posts, Twitter threads, and blog articles.
Ensure both `ffmpeg` and `ffprobe` are installed and added to your system's `PATH`.
